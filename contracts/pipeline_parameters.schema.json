{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "contracts/pipeline_parameters.schema.json",
  "title": "PhotoSorter Pipeline Parameters",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "device": {
      "type": "string",
      "enum": ["auto", "cpu", "mps"],
      "default": "auto"
    },
    "batch_size": {
      "type": "integer",
      "minimum": 1,
      "default": 16
    },
    "pooling": {
      "type": "string",
      "enum": ["cls", "avg", "cls+avg"],
      "default": "avg"
    },
    "preprocess": {
      "type": "string",
      "enum": ["letterbox", "timm"],
      "default": "letterbox"
    },
    "distance_threshold": {
      "type": "number",
      "exclusiveMinimum": 0,
      "default": 0.2
    },
    "linkage": {
      "type": "string",
      "enum": ["average", "complete", "single"],
      "default": "complete"
    },
    "temporal_weight": {
      "type": "number",
      "minimum": 0,
      "default": 0.0
    }
  }
}
